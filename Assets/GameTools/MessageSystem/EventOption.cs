namespace GameTools.MessageSystem
{
    /*
     * Use of enum in place of string can improve identification of valid events
     * 
     */
    public enum EventOption
    {
        Test,
        Spin,
        ETC,
    }
}